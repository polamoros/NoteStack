- when switching to sign up, the form is moving up
- if loading the sign in, and I click sign up, the form is showing "signing up"
- * the style of the section when draggin is weird. once i leave the section header is not preserving the position

* add configurable icon per stack (and automatically select by name with some nice logic)

* the input of the new stack styles are not nice
